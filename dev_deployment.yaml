---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "auto-service-helm-dev-devcommunity"
  namespace: dev
  labels:
    app: "auto-service-helm-dev-devcommunity"
---
apiVersion: v1
kind: Service
metadata:
  name: "auto-service-helm-dev-devcommunity"
  namespace: "dev"
  labels:
    app: "auto-service-helm-dev-devcommunity"
spec:
  selector:
    app: "auto-service-helm-dev-devcommunity"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 1001
---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "auto-service-helm-dev-devcommunity"
  namespace: "dev"
  labels:
    app: "auto-service-helm-dev-devcommunity"
  annotations:
    timestamp: "20250509T115512"
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: "100%"
      maxUnavailable: "5%"
    type: "RollingUpdate"
  minReadySeconds: 0
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: "auto-service-helm-dev-devcommunity"
  template:
    metadata:
      labels:
        app: "auto-service-helm-dev-devcommunity"
      annotations:
        timestamp: "20250509T115512"
    spec:
      serviceAccountName: "auto-service-helm-dev-devcommunity"
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: "ot-nexus-regcred"
      containers:
        - name: "auto-service-helm-dev-devcommunity"
          image: "registry.buildpiper.in/automation-682046mu117xjpt/dev/devcommunity:${BP_IMAGE_TAG}"
          imagePullPolicy: "Always"
          command: null
          args: null
          ports:
            - containerPort: 1001
              protocol: "TCP"
              name: "ot"
          envFrom: null
          resources:
            limits:
              cpu: "100m"
              memory: "100Mi"
            requests:
              cpu: "100m"
              memory: "100Mi"
          livenessProbe: null
          readinessProbe: null
          volumeMounts: null
      nodeSelector: {}
      affinity: {}
      tolerations: []
      volumes: null
